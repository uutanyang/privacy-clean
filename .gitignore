node_modules/
dist/
.wrangler/
.dev.vars

# ── Dot files (hidden/system) ──
.codebuddy/
.DS_Store
.env
.env.*
.vscode/
.idea/

# ── Privacy & secrets ──
.dev.vars
*.pem
*.key
*.secret
credentials.json

# ── WASM build artifacts ──
# wasm/target/ is the Rust build cache — too large, regenerated locally
wasm/target/
# NOTE: public/wasm/*.wasm is intentionally NOT ignored — it's the pre-compiled
# WASM binary needed at runtime. Cloudflare Pages build doesn't have a Rust
# toolchain, so the compiled .wasm must be committed to the repo.
